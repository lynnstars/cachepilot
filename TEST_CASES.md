# CachePilot 测试案例（Step2 固化版）

> 原则：先小样本真机验证 → 你确认 → 再铺全量。
> 安全承诺：清理 = 移入废纸篓（可恢复），绝不 rm。

## TC-0 工具自检
| # | 步骤 | 预期 |
|---|---|---|
| 0.1 | `python3 scanner/scan.py`（dry-run） | 输出分组清单，末尾标注"本次为只读扫描，未做任何删除" |
| 0.2 | 重复执行 0.1 | 两次输出一致（扫描可重复、无副作用） |

## TC-1 安全护栏
| # | 场景 | 预期 |
|---|---|---|
| 1.1 | `--trash` 不带 `--ids` | 仅处理 risk=low 且 default_clean=true 的项；medium/high/show_only 一律不动 |
| 1.2 | 指定 ollama 模型（high）`--ids ollama-models --trash` | 拒绝执行（护栏拦截） |
| 1.3 | 执行后检查 `~/.Trash/xxx-CachePilot-时间戳` | 文件在废纸篓内，可从废纸篓拖回原路径 |
| 1.4 | 对空目录/已不存在路径执行 | 静默跳过，不报错不误删 |
| 1.5 | 终端无"完全磁盘访问"权限时 | 读 ~/.Trash 会被 TCC 拒绝；恢复用 Finder（osascript）代为 move + rename（2026-09-09 实测有效） |

## TC-2 规则真机验证（linzsy 本机现状）
> 2026-09-03 缓存刚清过，多数项 < 阈值；后续用"养一段时间"或换台典型机器复测。

| 规则 id | 当前真机预期 | 验证方法 |
|---|---|---|
| derived-data | 0.28 GB 可识别 | dry-run 出现 → 授权后 `--ids derived-data --trash` → 废纸篓可见 → Xcode 下次编译自动重建 |
| hf-cache | 0.60 GB（~/.cache/huggingface/hub） | 仅展示为 🟡，默认不勾；dry-run 可见 |
| ollama-models | 6.5 GB（演示机另一账号下） | 仅展示 🔴 + 模型清单，任何情况下不可一键清 |
| npm/pip/uv/brew/cargo… | 刚清过，预期 0/低 | dry-run 不出现或低于阈值 → 说明"已清理态"识别正确 |
| 剪映/playwright/camoufox | 刚清过，预期 0/低 | 同上 |

## TC-3 规则库数据正确性（人造样本，离线可测）
| # | 样本 | 预期 |
|---|---|---|
| 3.1 | 造一个 60MB 假 `~/.cache/uv/fake` | dry-run 出现 uv 缓存 ~60MB |
| 3.2 | 把假样本删除 | dry-run 消失 |
| 3.3 | 规则 JSON 缺字段 | 扫描器不崩，跳过该规则并提示 |

## TC-4 UI（SwiftUI app 阶段执行）
| # | 步骤 | 预期 |
|---|---|---|
| 4.1 | 打开 app → 扫描 | 分组与原型一致，大小格式无长尾小数 |
| 4.2 | 勾选 → 移入废纸篓 | 顶部空间数字实时变化，toast 提示可恢复 |
| 4.3 | 清空勾选 → 按钮禁用 | 无 0GB 误操作 |
| 4.4 | 仅展示项（ollama/node_modules） | 无勾选框或置灰 |

## TC-5 异常与边界
| # | 场景 | 预期 |
|---|---|---|
| 5.1 | 磁盘只剩 <2GB 时扫描 | 扫描仍可完成，提示空间紧张 |
| 5.2 | 目标路径被占用/无权限 | 跳过并提示，不中断 |
| 5.3 | 连续两次 --trash 同一规则 | 第二次无文件可移（幂等） |

---
**执行记录**（填日期/结果/批准人）
- [ ] TC-0 通过
- [ ] TC-1 通过（需一次真机 low 项授权验证）
- [ ] TC-2 通过（linzsy 现状快照）
- [ ] TC-4 通过（SwiftUI 完成后）
